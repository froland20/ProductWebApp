<nav class="bg-gray-800 p-4 text-white">
    <div class="container mx-auto flex justify-between items-center">
        <a href="{{ path('homepage') }}" class="text-2xl font-bold">
            Symfony Login
        </a>
        <div class="space-x-8">
            <a href="{{ path('homepage') }}" class="hover:text-gray-400">Home</a>
            <a href="{{ path('homepage') }}" class="hover:text-gray-400">About</a>
            <a href="{{ path('homepage') }}" class="hover:text-gray-400">Contact</a>
        </div>
        <div class="space-x-4">
            {% if app.user %}
                <a href="{{ path('app_logout') }}" class="hover:text-gray-400">Manage</a>
                <a href="{{ path('app_logout') }}" class="hover:text-gray-400">Logout</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="hover:text-gray-400">Login</a>
            {% endif %}
        </div>
    </div>
</nav>
